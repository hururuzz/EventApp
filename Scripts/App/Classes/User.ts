namespace EventApp{
    class User {
        userId: number;
        userName: string;
        email: string;
        password: string;
    }

    function ViewUserEvents(userId: number): Event{
        var userEvents: Event;
        
        return userEvents;
    }
}