"use strict"

var emailer = require("nodemailer");

var emailTransporter = emailer.createTransport('smtps://blackmailappcpsc473%40@gmail.com:cpsc12345!@smtp.gmail.com');  

this.setEmailContents = function(receiver, title, body) {
    return {
        from: 'Event App',
        to: receiver,
        subject: title,
        html: body
    }
}

this.sendEmail = function(mailOptions){
    emailTransporter.sendMail(mailOptions, function(error, info){
        if(error){
            return console.log(error);
        } 
        console.log('New password sent');
    }); 
}
